<div
  class="aether-announcement-bar ph-client ph-client--aether{% if section.settings.show_mobile_rotation and section.blocks.size > 1 %} aether-announcement-bar--mobile-rotation{% endif %}"
  data-section-type="aether-announcement-bar"
  data-aether-motion-enabled="{{ settings.aether_motion_enable }}"
>
  <div class="announcement-content">
    {%- for block in section.blocks -%}
      {%- if forloop.first != true -%}
        <span class="separator" aria-hidden="true">|</span>
      {%- endif -%}
      <span {{ block.shopify_attributes }}>
        <i class="fas {{ block.settings.icon }}" aria-hidden="true"></i>
        {{ block.settings.text | escape }}
      </span>
    {%- endfor -%}
  </div>

  {%- if section.settings.show_mobile_rotation and section.blocks.size > 1 -%}
    <div class="aether-mobile-announcement" aria-hidden="true">
      {%- for block in section.blocks -%}
        <span class="aether-mobile-announcement-text{% if forloop.first %} active{% endif %}">{{ block.settings.text | escape }}</span>
      {%- endfor -%}
    </div>
  {%- endif -%}
</div>

{% schema %}
  {
    "name": "t:sections.aether-announcement-bar.name",
    "settings": [
      {
        "type": "checkbox",
        "id": "show_mobile_rotation",
        "default": true,
        "label": "t:sections.aether-announcement-bar.settings.show_mobile_rotation.label"
      }
    ],
    "blocks": [
      {
        "type": "message",
        "name": "t:sections.aether-announcement-bar.blocks.message.name",
        "settings": [
          {
            "type": "select",
            "id": "icon",
            "options": [
              {
                "value": "fa-truck",
                "label": "t:sections.aether-announcement-bar.blocks.message.settings.icon.options.fa-truck"
              },
              {
                "value": "fa-bolt",
                "label": "t:sections.aether-announcement-bar.blocks.message.settings.icon.options.fa-bolt"
              },
              {
                "value": "fa-undo",
                "label": "t:sections.aether-announcement-bar.blocks.message.settings.icon.options.fa-undo"
              }
            ],
            "default": "fa-truck",
            "label": "t:sections.aether-announcement-bar.blocks.message.settings.icon.label"
          },
          {
            "type": "text",
            "id": "text",
            "default": "Free shipping on orders over $200",
            "label": "t:sections.aether-announcement-bar.blocks.message.settings.text.label"
          }
        ]
      }
    ],
    "max_blocks": 3,
    "presets": [
      {
        "name": "t:sections.aether-announcement-bar.presets.default.name"
      }
    ],
    "disabled_on": {
      "groups": ["header"]
    }
  }
{% endschema %}
